package com.clarysse.jarne.university_go;

import android.widget.Button;
import android.widget.EditText;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;


import static junit.framework.Assert.assertEquals;
import static junit.framework.Assert.assertNotNull;


//@RunWith(RobolectricTestRunner.class)
public class registrationTest
{/*
    RegisterActivity activity;


    @Before
    public void setUp() throws Exception
    {
        activity = Robolectric.buildActivity( RegisterActivity.class )
                .create()
                .resume()
                .get();
    }

    @Test
    public void checkActivityNotNull() throws Exception {
        assertNotNull(fragment);
    }

    @Test
    public void testSlechteToevoeging(){

        EditText username=(EditText)fragment.getView().findViewById(R.id.register_username_field);
        EditText password=(EditText)fragment.getView().findViewById(R.id.register_password_field);
        EditText email=(EditText)fragment.getView().findViewById(R.id.register_email_field);
        username.setText("test");
        password.setText("1");

        //Kies voor een leeg email-veld
        email.setText("");

        Button button=(Button) fragment.getView().findViewById(R.id.registerbutton);
        button.performClick();

    }

    @Test
    public void goodRegistration(){
        EditText username=(EditText)fragment.getView().findViewById(R.id.register_username_field);
        EditText password=(EditText)fragment.getView().findViewById(R.id.register_password_field);
        EditText email=(EditText)fragment.getView().findViewById(R.id.register_email_field);
        username.setText("test");
        password.setText("1");
        email.setText("test");

        Button button=(Button) fragment.getView().findViewById(R.id.registerbutton);
        button.performClick();

    }


*/
}